import { styled } from '~/config/theme';

export const ButtonStyled = styled('button', {
  backgroundColor: '',
  borderRadius: '9999px',
  fontSize: '14px',
  '&:hover': {
    backgroundColor: 'AliceBlue',
  },
});
